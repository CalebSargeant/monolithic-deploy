host = {{ fortivpn_host }}
port = {{ fortivpn_port }}
username = {{ fortivpn_username }}
password = {{ fortivpn_password }}
trusted-cert = {{ fortivpn_cert }}
set-dns = 0
use-resolvconf = 1
set-routes = 1