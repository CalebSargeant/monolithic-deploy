apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana
  namespace: observability
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: grafana-config
